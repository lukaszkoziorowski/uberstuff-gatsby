import React from "react";
import styles from "./WorkInProgress.module.scss";

const WorkInProgress = () => {
  return <h1 className={styles.WorkInProgress}>WORK IN PROGRESS</h1>;
};

export default WorkInProgress;
